"""Top-level convenience module (flat layout alongside src/)."""


def cli_entry(argv: list[str] | None = None) -> int:
    """Run the demo CLI.

    Args:
        argv: Arguments; defaults to sys.argv.

    Returns:
        Process exit code.
    """
    return 0
