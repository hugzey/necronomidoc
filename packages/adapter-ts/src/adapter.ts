// The adapter contract moved to @necronomidoc/docmodel (shared by all adapter
// packages); re-exported here so existing imports keep working.
export type { AdapterConfig, AdapterMatch, DocAdapter } from "@necronomidoc/docmodel";
